- title: DCAT-AP-NL with OGC API Records in JSON
  content: |-
    An example based on the Bomen metadata record . 
    Converted in JSON so the Semantic uplift via a JSON-LD context can be shown.

  snippets:
    - language: json
      ref: examples/bomen.geojson